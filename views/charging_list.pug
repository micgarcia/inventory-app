extends layout

block content
  div.chargingListContent
    h1= title

    ul
      each product in charging_list
        li.chargingListItem
          a(href=product.url) #{product.name}
          p Price: $#{product.price}

    a(href="/catalog/charging/create") Add New Charging Product
